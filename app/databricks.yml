bundle:
  name: "dqx-steve"

sync:
  include:
    - .build

artifacts:
  default:
    build: .venv/bin/python -m apx build && uv build --wheel --out-dir ".build" ".."

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405612347557713.13.azuredatabricks.net/
